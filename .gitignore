# build output
dist/
.astro/

# dependencies
node_modules/

# env
.env
.env.local
.env.production
.env.development

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# build cache
.cache/
.tmp/

# Claude Code internal state (never commit)
.claude/

# Auto-generated operator inbox (postbuild) — local-only signal
OPERATOR_INBOX.md

# Agent runtime scratch (heartbeats, review reports) — keep dir via .gitkeep
tmp/
!tmp/.gitkeep

# fact-check-queue/ 의 *.json 은 워크플로우가 commit (data-queue/ 와 동일 패턴) — gitignore 하지 않는다.
